<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home">

    <fragment
        android:id="@+id/home"
        android:name="burujiyaseer.example.tmdbapp.ui.home.HomeFragment"
        android:label="@string/home"
        tools:layout="@layout/fragment_home">
    </fragment>

    <fragment
        android:id="@+id/discover"
        android:name="burujiyaseer.example.tmdbapp.ui.discover.DiscoverFragment"
        android:label="@string/explore"
        tools:layout="@layout/fragment_discover">
    </fragment>

    <fragment
        android:id="@+id/favorite"
        android:name="burujiyaseer.example.tmdbapp.ui.favorites.FavoritesFragment"
        android:label="@string/explore"
        tools:layout="@layout/fragment_favorites">
    </fragment>
</navigation>